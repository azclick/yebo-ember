<nav class="top-bar">
  <section class="top-bar-section">
    <ul>
      <li>
        {{#link-to 'yebo.index'}}
          Yebo Ember
        {{/link-to}}
      </li>
    </ul>
    <ul class="right">
      {{#if session}}
        <li>
          {{#if session.isAuthenticated}}
            {{#link-to 'yebo.account'}}Account{{/link-to}}
          {{else}}
            {{#link-to 'yebo.signin'}}Login{{/link-to}}
          {{/if}}
        </li>
      {{/if}}
      <li>
        {{#link-to 'yebo.cart'}}
          Cart: &nbsp;
          {{#if yebo.currentOrder}}
            {{yebo.currentOrder.itemCount}}
          {{else}}
             0
          {{/if}}
        {{/link-to}}
      </li>
    </ul>
  </section>
</nav>
